<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>猫狗大战</title>
      <link href="/posts/eb14.html"/>
      <url>/posts/eb14.html</url>
      
        <content type="html"><![CDATA[<h1 id="猫狗大战"><a href="#猫狗大战" class="headerlink" title="猫狗大战"></a>猫狗大战</h1><p>通过来自 kaggle 上的猫狗数据集，训练一个识别猫狗图片的分类器。要求 设计一个使用 ResNet18 作为主干的卷积神经网络，在迁移网络时采用固定值模 式，要求模型的准确率不低于 90%。猫狗大战数据集训练集有 25000 张，猫狗各 占一半。测试集 12500 张。</p><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport torch.nn as nnimport torch. nn.functional as Ffrom torch.utils.data import Dataset, DataLoader, TensorDatasetimport matplotlib.pyplot as pltimport numpy as npimport timefrom torch.utils.data.sampler import SubsetRandomSamplerfrom torchvision import models, datasets, transformsfrom PIL import Image<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>由于给定的数据集，缺少label，故需要自定义数据集，并打上标签，因为数据集里的图片以cat_xx、dog_xx命名，那么就可以根据图片名称来打上label，例如cat:0、dog:1</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">class MyDataset(Dataset):    def __init__(self, root_path, transform=None):        self.label_name = {"Cat": 0, "Dog": 1}        self.root_path = root_path        self.transform = transform        self.get_train_img_info()    def __getitem__(self, index):        self.img = Image.open(os.path.join(self.root_path, self.train_img_name[index]))        if self.transform is not None:            self.img = self.transform(self.img)        self.label = self.train_img_label[index]        return self.img, self.label    def __len__(self):        return len(self.train_img_name)    def get_train_img_info(self):        self.train_img_name = os.listdir(self.root_path)        self.train_img_label = [0 if 'cat' in imgname else 1 for imgname in self.train_img_name]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>同样划分成训练集、验证集、测试集，并且对数据集进行数据增强，如随机翻转，改变尺寸等</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">def gen_data():    transform_train = transforms.Compose([                                          transforms.RandomHorizontalFlip(),                                          transforms.RandomGrayscale(),                                          transforms.Resize([224,224]),                                          transforms.ToTensor(),                                          transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])                                      ])                                    transform_test = transforms.Compose([                                          transforms.Resize([224,224]),                                          transforms.ToTensor(),                                          transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])                                      ])         train_data = MyDataset(root_path=os.path.join(os.getcwd(), 'datasets/train'), transform=transform_train)    test_data = MyDataset(root_path=os.path.join(os.getcwd(), 'datasets/test'), transform=transform_test)    num_train = len(train_data)    indices = list(range(num_train))    np.random.shuffle(indices)    split = int(np.floor(0.2 * num_train))    train_idx, valid_idx = indices[split:], indices[:split]    # define samplers for obtaining training and validation batches    train_sampler = SubsetRandomSampler(train_idx)    valid_sampler = SubsetRandomSampler(valid_idx)    train_loader = DataLoader(dataset=train_data, batch_size=64, sampler=train_sampler)    valid_loader = DataLoader(dataset=train_data, batch_size=64, sampler=valid_sampler)    test_loader = DataLoader(dataset=test_data, batch_size=64)    print(len(test_data))    img = test_data[10][0]    plt.imshow(img[0, :])    print(test_data[10][1])    return train_loader, valid_loader, test_loader<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>数据集图片如下图所示</p><p><img src="/posts/eb14/image-20230110173807327.png" alt="数据集中的图片"></p><h2 id="2-建立模型"><a href="#2-建立模型" class="headerlink" title="2.建立模型"></a>2.建立模型</h2><p>应用迁移学习，主干网络选用resnet18，从而只需将resnet18的最后一层全连接层改为输出只有2的全连接层</p><pre class="line-numbers language-lang-python"><code class="language-lang-python"># 加载resnet18作为预训练模型net = models.resnet18(pretrained=True)# 冻结参数for param in net.parameters():    param.requires_grad = False# 改造最后一层，输出改为两个神经元net.fc = nn.Linear(net.fc.in_features, 2)# 在GPU上训练if torch.cuda.is_available():    net = net.cuda()    net.fc = net.fc.cuda()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(train_loader, valid_loader, net, loss_func, opt, epochs):    valid_loss_min = np.Inf        start = time.time()    train_loss_list = []    valid_loss_list = []    for epoch in range(epochs):        train_loss = 0.0        valid_loss = 0.0        net.train()        for x, y in train_loader:            x = x.cuda()            y = y.cuda()            pred = net(x)            loss = loss_func(pred, y)            opt.zero_grad()            loss.backward()            opt.step()            train_loss += loss.item() * x.size(0)        net.eval()        for x, y in valid_loader:            x = x.cuda()            y = y.cuda()            pred = net(x)            loss = loss_func(pred, y)            valid_loss += loss.item() * x.size(0)        # 计算平均损失        train_loss = train_loss/len(train_loader.sampler)        valid_loss = valid_loss/len(valid_loader.sampler)        train_loss_list.append(train_loss)        valid_loss_list.append(valid_loss)        # 显示训练集与验证集的损失函数         print('Epoch: {} \tTraining Loss: {:.6f} \tValidation Loss: {:.6f}'.format(            epoch + 1, train_loss, valid_loss))        # 如果验证集损失函数减少，就保存模型。        if valid_loss <= valid_loss_min:            print('Validation loss decreased ({:.6f} --> {:.6f}).  Saving model ...'.format(valid_loss_min,valid_loss))            torch.save(net.state_dict(), 'cat_vs_dog.pt')            valid_loss_min = valid_loss        print("spend times: ", time.time() - start)    plt.plot(np.arange(epochs), train_loss_list, 'r', label='train')    plt.plot(np.arange(epochs), valid_loss_list, 'b', label='valid')    plt.xlabel('epochs')    plt.ylabel('loss')    plt.legend()    plt.show()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">train_loader, valid_loader, test_loader = gen_data()loss_func = nn.CrossEntropyLoss()opt = torch.optim.Adam(net.fc.parameters(), lr=0.001)epochs = 30train(train_loader, valid_loader, net, loss_func, opt, epochs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/eb14/image-20230110180145989.png" alt="训练误差"></p><h2 id="4-预测结果"><a href="#4-预测结果" class="headerlink" title="4.预测结果"></a>4.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(test_loader, net):    rights = 0    length = 0    for i, data in enumerate(test_loader):        x, y = data        x = x.cuda()        y = y.cuda()        pred = net(x)        rights += rightness(pred, y)[0]        length += rightness(pred, y)[1]    print(rights, length, 'acc: {:.6f}'.format(rights / length))def rightness (pred, labels):    pred = torch.max(pred.data, 1)[1]    rights = pred.eq(labels.data.view_as(pred)).sum()    return rights, len(labels)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">net.load_state_dict(torch.load('cat_vs_dog.pt'))prediction(test_loader, net)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><img src="/posts/eb14/image-20230110180245252.png" alt="准确率"></p>]]></content>
      
      
      <categories>
          
          <category> 迁移学习 </category>
          
          <category> 卷积神经网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>正弦函数预测</title>
      <link href="/posts/9233.html"/>
      <url>/posts/9233.html</url>
      
        <content type="html"><![CDATA[<h1 id="正弦函数预测"><a href="#正弦函数预测" class="headerlink" title="正弦函数预测"></a>正弦函数预测</h1><p>通过已知的样本数据对正弦函数进行预测，并绘制出图形。设计 LSTM网络进行预测，记录预测准确率并绘制图形。</p><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport torchvision.datasets as datasetsimport torch.nn as nnimport torch. nn.functional as Ffrom torch.utils.data import Dataset, DataLoader, TensorDatasetimport matplotlib.pyplot as pltimport numpy as npimport time<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">def getSeq(start, n):    x = [np.sin(x / 10) for x in range(start, start + n)]    return xdef gen_data():    data = []    for i in range(200):        data.append(getSeq(i, 11))    data = torch.from_numpy(np.array(data))    data = data[:, : -1].type(torch.FloatTensor)    target = data[:, -1 :].type(torch.FloatTensor)    train_x = data[: 150]    train_y = target[: 150]    test_x = data[150 :]    test_y = target[150 :]    train_dataset = TensorDataset(train_x, train_y)    test_dataset = TensorDataset(test_x, test_y)    train_loader = DataLoader(dataset=train_dataset, batch_size=5, shuffle=True)    test_loader = DataLoader(dataset=test_dataset, batch_size=5, shuffle=False)    return train_y, train_loader, test_loader<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="2-构建模型"><a href="#2-构建模型" class="headerlink" title="2.构建模型"></a>2.构建模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">class LSTM(nn.Module):    def __init__(self):        super().__init__()        self.lstm = nn.LSTM(1, 10, batch_first=True)        self.fc = nn.Linear(10, 1)    def forward(self, x, hidden):        output, hidden = self.lstm(x, hidden)        output = output[:, -1, :]        output = self.fc(output)        return output<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(train_loader, lstm, loss_func, opt, epochs):            h0 = torch.zeros(1, 5, 10)    c0 = torch.zeros(1, 5, 10)    train_loss_list = []    for epoch in range(epochs):        for i, data in enumerate(train_loader):            x, y = data            x = x.view(-1, 10, 1)            pred = lstm(x, (h0, c0))            loss = loss_func(pred, y)            opt.zero_grad()            loss.backward()            opt.step()        train_loss_list.append(loss.detach().numpy())        if epoch % 100 == 0:            print("LSTM_loss: ", loss)    plt.plot(np.arange(epochs), train_loss_list, 'r', label='train')    plt.xlabel('epochs')    plt.ylabel('loss')    plt.legend()    plt.show()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">lstm = LSTM()loss_func = nn.MSELoss()opt = torch.optim.Adam(lstm.parameters(), lr=0.001)epochs = 500train_y, train_loader, test_loader = gen_data()train(train_loader, lstm, loss_func, opt, epochs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/9233/image-20230110173005210.png" alt="训练损失"></p><h2 id="4-预测结果"><a href="#4-预测结果" class="headerlink" title="4.预测结果"></a>4.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(test_loader, lstm):    h0 = torch.zeros(1, 5, 10)    c0 = torch.zeros(1, 5, 10)    preds = []    for i, data in enumerate(test_loader):        x, y = data        x = x.view(-1, 10, 1)        pred = lstm(x, (h0, c0))        preds.append(pred.detach().numpy())#         print(y.view(1, -1))#         print(pred.view(1, -1))    true = plt.scatter(range(len(train_y)), train_y.detach().numpy(), marker='o')    lstm_pred = plt.scatter(range(150, 200), preds, marker='s')    plt.legend((true, lstm_pred),('true','lstm_pred') ,loc = 'best')       plt.xlabel('x')    plt.ylabel('y')    plt.show()prediction(test_loader, lstm)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/9233/image-20230110173238553.png" alt="预测结果"></p>]]></content>
      
      
      <categories>
          
          <category> LSTM </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CIFAR-10分类网络</title>
      <link href="/posts/8865.html"/>
      <url>/posts/8865.html</url>
      
        <content type="html"><![CDATA[<h1 id="CIFAR-10分类网络"><a href="#CIFAR-10分类网络" class="headerlink" title="CIFAR-10分类网络"></a>CIFAR-10分类网络</h1><p>通过 CIFAR-10 数据集训练得到一个彩色图像分类网络。要求设计一 个至少包含 5 个卷积层和池化层的卷积神经网络。卷积核的尺寸统一采用 3*3， 要求训后的得到的网络在测试集上的准确率不低于 70%（要求在网络中使用 BatchNorm）</p><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport torchvision.datasets as datasetsimport torchvision.transforms as transformsimport torch.nn as nnimport torch. nn.functional as Ffrom torch.utils.data import Dataset, DataLoader, TensorDatasetimport matplotlib.pyplot as pltimport numpy as npimport timefrom torch.utils.data.sampler import SubsetRandomSampler<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">train_data = datasets.CIFAR10(root='./',                      train=True,                      transform=transforms.Compose([                          transforms.RandomHorizontalFlip(),                          transforms.RandomGrayscale(),                          transforms.ToTensor(),                          transforms.Normalize((0.5, 0.5, 0.5), (0.5, 0.5, 0.5))                      ]),                      download=True                     )test_data = datasets.CIFAR10(root='./',                      train=False,                      transform=transforms.Compose([                          transforms.ToTensor(),                          transforms.Normalize((0.5, 0.5, 0.5), (0.5, 0.5, 0.5))                      ])                     )num_train = len(train_data)indices = list(range(num_train))np.random.shuffle(indices)split = int(np.floor(0.2 * num_train))train_idx, valid_idx = indices[split:], indices[:split]# define samplers for obtaining training and validation batchestrain_sampler = SubsetRandomSampler(train_idx)valid_sampler = SubsetRandomSampler(valid_idx)train_loader = DataLoader(dataset=train_data, batch_size=64, sampler=train_sampler, num_workers=8)valid_loader = DataLoader(dataset=train_data, batch_size=64, sampler=valid_sampler, num_workers=8)test_loader = DataLoader(dataset=test_data, batch_size=64, num_workers=8)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="构建模型"><a href="#构建模型" class="headerlink" title="构建模型"></a>构建模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">class model(nn.Module):    def __init__(self):        super().__init__()        self.conv1 = nn.Conv2d(3, 16, 3, padding=1)        self.conv2 = nn.Conv2d(16, 32, 3, padding=1)        self.conv3 = nn.Conv2d(32, 64, 3, padding=1)        self.conv4 = nn.Conv2d(64, 64, 3, padding=1)        self.conv5 = nn.Conv2d(64, 64, 3, padding=1)        self.bn1 = nn.BatchNorm2d(16)        self.bn2 = nn.BatchNorm2d(32)        self.bn3 = nn.BatchNorm2d(64)        self.bn4 = nn.BatchNorm2d(64)        self.bn5 = nn.BatchNorm2d(64)        self.pool = nn.MaxPool2d(2, 2)        self.fc1 = nn.Linear(64 * 1 * 1, 500)        self.fc2 = nn.Linear(500, 10)    def forward(self, x):        x = self.conv1(x)        x = self.bn1(x)        x = F.relu(x)        x = self.pool(x)        x = self.conv2(x)        x = self.bn2(x)        x = F.relu(x)        x = self.pool(x)        x = self.conv3(x)        x = self.bn3(x)        x = F.relu(x)        x = self.pool(x)              x = self.conv4(x)        x = self.bn4(x)        x = F.relu(x)        x = self.pool(x)           x = self.conv5(x)        x = self.bn5(x)        x = F.relu(x)        x = self.pool(x)           x = x.view(-1, 64 * 1 * 1)        x = self.fc1(x)        x = F.relu(x)        x = self.fc2(x)        return x<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>用BN代替dropout</p></blockquote><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(train_loader, valid_loader, net, loss_func, opt, epochs):    valid_loss_min = np.Inf    start = time.time()    train_loss_list = []    valid_loss_list = []    for epoch in range(epochs):        train_loss = 0.0        valid_loss = 0.0        net.train()        for x, y in train_loader:            x = x.cuda()            y = y.cuda()            pred = net(x)            loss = loss_func(pred, y)            opt.zero_grad()            loss.backward()            opt.step()            train_loss += loss.item() * x.size(0)        net.eval()        for x, y in valid_loader:            x = x.cuda()            y = y.cuda()            pred = net(x)            loss = loss_func(pred, y)            valid_loss += loss.item() * x.size(0)        # 计算平均损失        train_loss = train_loss/len(train_loader.sampler)        valid_loss = valid_loss/len(valid_loader.sampler)        train_loss_list.append(train_loss)        valid_loss_list.append(valid_loss)        # 显示训练集与验证集的损失函数         print('Epoch: {} \tTraining Loss: {:.6f} \tValidation Loss: {:.6f}'.format(            epoch, train_loss, valid_loss))        # 如果验证集损失函数减少，就保存模型。        if valid_loss <= valid_loss_min:            print('Validation loss decreased ({:.6f} --> {:.6f}).  Saving model ...'.format(valid_loss_min,valid_loss))            torch.save(net.state_dict(), 'model_cifar.pt')            valid_loss_min = valid_loss        print('spend_time: ', time.time() - start)    plt.plot(np.arange(epochs), train_loss_list, 'r', label='train')    plt.plot(np.arange(epochs), valid_loss_list, 'b', label='valid')    plt.xlabel('epochs')    plt.ylabel('loss')    plt.legend()    plt.show()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">cnn = model().cuda()loss_func = nn.CrossEntropyLoss()opt = torch.optim.SGD(cnn.parameters(), lr = 0.01, momentum=0.9)epochs = 10train(train_loader, valid_loader, cnn, loss_func, opt, epochs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/8865/image-20230110171655783.png" alt="训练损失"></p><blockquote><p>使用验证集，可挑选泛化能力更好的模型</p></blockquote><h2 id="4-预测结果"><a href="#4-预测结果" class="headerlink" title="4.预测结果"></a>4.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(test_loader, net):    rights = 0    length = 0    for i, data in enumerate(test_loader):        x, y = data        x = x.cuda()        y = y.cuda()        pred = net(x)        rights += rightness(pred, y)[0]        length += rightness(pred, y)[1]    print(rights / length)def rightness (pred, labels):    pred = torch.max(pred.data, 1)[1]    rights = pred.eq(labels.data.view_as(pred)).sum()    return rights, len(labels)       cnn.load_state_dict(torch.load('model_cifar.pt'))prediction(test_loader, cnn)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/8865/image-20230110171751740.png" alt="准确率"></p>]]></content>
      
      
      <categories>
          
          <category> 卷积神经网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>手写数字识别器</title>
      <link href="/posts/b31.html"/>
      <url>/posts/b31.html</url>
      
        <content type="html"><![CDATA[<h1 id="手写数字识别器"><a href="#手写数字识别器" class="headerlink" title="手写数字识别器"></a>手写数字识别器</h1><p>通过 MNIST 数据集训练得到一个手写数字分类器。要求设计一个至 少包含 2 个卷积层和池化层的卷积神经网络。卷积核的尺寸不小于 5*5，要求训 后的得到的网络在测试集确率不低于 96%（要求在网络中使用 dropout）</p><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport torchvision.datasets as datasetsimport torchvision.transforms as transformsimport torch.nn as nnimport torch. nn.functional as Ffrom torch.utils.data import Dataset, DataLoader, TensorDatasetimport matplotlib.pyplot as pltimport numpy as npimport timefrom torch.utils.data.sampler import SubsetRandomSampler<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">train_data = datasets.MNIST(root='./',                      train=True,                      transform=transforms.Compose([                          transforms.ToTensor(),                          transforms.Normalize([0.1307,], [0.3081,])                      ]),                      download=True                     )test_data = datasets.MNIST(root='./',                      train=False,                      transform=transforms.Compose([                          transforms.ToTensor(),                          transforms.Normalize([0.1307,], [0.3081,])                      ])                     )num_train = len(train_data)indices = list(range(num_train))np.random.shuffle(indices)split = int(np.floor(0.2 * num_train))train_idx, valid_idx = indices[split:], indices[:split]# define samplers for obtaining training and validation batchestrain_sampler = SubsetRandomSampler(train_idx)valid_sampler = SubsetRandomSampler(valid_idx)train_loader = DataLoader(dataset=train_data, batch_size=64, sampler=train_sampler)valid_loader = DataLoader(dataset=train_data, batch_size=64, sampler=valid_sampler)test_loader = DataLoader(dataset=test_data, batch_size=64)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>这里增加了验证集，其目的是挑选泛化能力好的模型，并且在训练时保存起来</p></blockquote><h2 id="2-构建模型"><a href="#2-构建模型" class="headerlink" title="2.构建模型"></a>2.构建模型</h2><p>两个卷积层和最大池化层，最后要将输出展平，作为全连接的输入，输入的尺寸需要计算</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">class model(nn.Module):    def __init__(self):        super().__init__()        self.conv1 = nn.Conv2d(1, 4, 5, padding=2)        self.pool = nn.MaxPool2d(2, 2)        self.conv2 = nn.Conv2d(4, 8, 5 ,padding=2)        self.fc1 = nn.Linear((28 * 28) // (4 * 4) * 8, 512)        self.fc2 = nn.Linear(512, 10)    def forward(self, x):        x = self.conv1(x)        x = F.relu(x)        x = self.pool(x)        x = self.conv2(x)        x = F.relu(x)        x = self.pool(x)        x = x.view(-1, (28 * 28) // (4 * 4) * 8)        x = self.fc1(x)        x = F.relu(x)        x = F.dropout(x, training=self.training)        x = self.fc2(x)        return x    def feature_map(self, x):        map1 = self.conv1(x)        map1 = F.relu(map1)        map2 = self.pool(map1)        map2 = self.conv2(map2)        map2 = F.relu(map2)        return map1, map2<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>这里运用了dropout，其目的是为了减少过拟合，feature_map方法返回卷积层的输出，绘制特征图</p></blockquote><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(train_loader, valid_loader, net, loss_func, opt, epochs):    valid_loss_min = np.Inf    start = time.time()    train_loss_list = []    valid_loss_list = []    for epoch in range(epochs):        train_loss = 0.0        valid_loss = 0.0        net.train()        for x, y in train_loader:            x = x.cuda()            y = y.cuda()            pred = net(x)            loss = loss_func(pred, y)            opt.zero_grad()            loss.backward()            opt.step()            train_loss += loss.item() * x.size(0)        net.eval()        for x, y in valid_loader:            x = x.cuda()            y = y.cuda()            pred = net(x)            loss = loss_func(pred, y)            valid_loss += loss.item() * x.size(0)        # 计算平均损失        train_loss = train_loss/len(train_loader.sampler)        valid_loss = valid_loss/len(valid_loader.sampler)        train_loss_list.append(train_loss)        valid_loss_list.append(valid_loss)        # 显示训练集与验证集的损失函数         print('Epoch: {} \tTraining Loss: {:.6f} \tValidation Loss: {:.6f}'.format(            epoch, train_loss, valid_loss))        # 如果验证集损失函数减少，就保存模型。        if valid_loss <= valid_loss_min:            print('Validation loss decreased ({:.6f} --> {:.6f}).  Saving model ...'.format(valid_loss_min,valid_loss))            torch.save(net.state_dict(), 'model_hnr.pt')            valid_loss_min = valid_loss        print('spend_time: ', time.time() - start)    plt.plot(np.arange(epochs), train_loss_list, 'r', label='train')    plt.plot(np.arange(epochs), valid_loss_list, 'b', label='valid')    plt.xlabel('epochs')    plt.ylabel('loss')    plt.legend()    plt.show()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">cnn = model().cuda() # GPU运行loss_func = nn.CrossEntropyLoss()opt = torch.optim.SGD(cnn.parameters(), lr = 0.001, momentum=0.9)epochs = 10train(train_loader, valid_loader, cnn, loss_func, opt, epochs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>model().cuda()可以将模型加载到GPU上训练，严重提高训练速度</p></blockquote><p><img src="/posts/b31/image-20230110164611331.png" alt="训练损失"></p><h2 id="4-预测结果"><a href="#4-预测结果" class="headerlink" title="4.预测结果"></a>4.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(test_loader, net):    rights = 0    length = 0    net.eval()    for i, data in enumerate(test_loader):        x, y = data        x = x.cuda()        y = y.cuda()        pred = net(x)        rights += rightness(pred, y)[0]        length += rightness(pred, y)[1]    print(rights / length)def rightness (pred, labels):    pred = torch.max(pred. data,1)[1]    rights = pred.eq(labels.data.view_as(pred)).sum()    return rights, len(labels)   cnn.load_state_dict(torch.load('model_h.pt'))prediction(test_loader, cnn)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/b31/image-20230110164708990.png" alt="准确率"></p><h2 id="5-输出特征图"><a href="#5-输出特征图" class="headerlink" title="5.输出特征图"></a>5.输出特征图</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def show(net):    for i in range(4):        plt.subplot(1, 4, i + 1)        plt.imshow(net.conv1.weight.cpu().detach().numpy()[i, 0, :])    plt.show()    x = train_data[50][0].unsqueeze(0).cuda()    map1, map2 = net.feature_map(x)    for i in range(4):        plt.subplot(1, 4, i + 1)        print(map1.detach().cpu().numpy().shape)        plt.imshow(map1.detach().cpu()[0, i, :].numpy())    plt.show()    for i in range(4):        plt.subplot(1, 4, i + 1)        plt.imshow(map2.detach().cpu()[0, i, :].numpy())    plt.show()show(cnn)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/b31/image-20230110164821922.png" alt="特征图"></p><blockquote><p>需要将GPU格式的数据转换为CPU格式的数据，即调用cpu()</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 卷积神经网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>鸢尾花分类</title>
      <link href="/posts/a174.html"/>
      <url>/posts/a174.html</url>
      
        <content type="html"><![CDATA[<h1 id="鸢尾花分类"><a href="#鸢尾花分类" class="headerlink" title="鸢尾花分类"></a>鸢尾花分类</h1><p>iris 数据集包含 150 个样本，对应数据集的每行数据。每行数据包含每个 样本的四个特征和样本的类别信息，iris 数据集是用来给鸢尾花做分类的数据集， 每个样本包含了花萼长度、花萼宽度、花瓣长度、花瓣宽度四个特征，请用神经 网络训练一个分类器，分类器可以通过样本的四个特征来判断样本属于山鸢尾、 变色鸢尾还是维吉尼亚鸢尾。数据集文件 iris.csv。要求模型准确率不低于 99%。</p><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport matplotlib.pyplot as pltimport numpy as npimport pandas as pdimport torch.nn as nnimport torch.nn.functional as Ffrom torch.utils.data import Dataset, DataLoader, TensorDatasetfrom sklearn.utils import shuffle<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">def gen_data(file):    data = pd.read_csv(file)    # 打上label    for i in range(len(data)):        if data.loc[i, 'Species'] == 'setosa':            data.loc[i, 'Species'] = 0        elif data.loc[i, 'Species'] == 'versicolor':            data.loc[i, 'Species'] = 1        else:            data.loc[i, 'Species'] = 2    data = data.drop('Unnamed: 0', axis=1)    data = shuffle(data)    data.index = range(len(data))    # 归一化    col_titles = ['Sepal.Length', 'Sepal.Width', 'Petal.Length', 'Petal.Width']    for i in col_titles:        mean, std = data[i].mean(), data[i].std()        data[i] = (data[i] - mean) / std    train_data = data[: -32]    train_x = train_data.drop(['Species'], axis=1).values    train_y = train_data['Species'].values.astype(int)    train_x = torch.from_numpy(train_x).type(torch.FloatTensor)    train_y = torch.from_numpy(train_y).type(torch.LongTensor)    test_data = data[-32: ]    test_x = test_data.drop(['Species'], axis=1).values    test_y = test_data['Species'].values.astype(int)    test_x = torch.from_numpy(test_x).type(torch.FloatTensor)    test_y = torch.from_numpy(test_y).type(torch.LongTensor)    train_dataset = TensorDataset(train_x, train_y)    test_dataset = TensorDataset(test_x, test_y)    train_loader = DataLoader(dataset=train_dataset, batch_size=32, shuffle=True)    test_loader = DataLoader(dataset=test_dataset, batch_size=16, shuffle=True)    return train_loader, test_loader<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="2-构建模型"><a href="#2-构建模型" class="headerlink" title="2.构建模型"></a>2.构建模型</h2><p>因为鸢尾花有四个特征，三个类别，所以构造模型如下</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">class model(torch.nn.Module):    def __init__(self):        super().__init__()        self.hidden = nn.Linear(4, 5) # 四个特征        self.out = nn.Linear(5, 3)    # 三个类别    def forward(self, x):        x = self.hidden(x)        x = F.relu(x)        x = self.out(x)        return x<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(net, loss_func, opt, epochs, train_loader):    losses = []    for epoch in range(epochs):        batch_loss = []        for i, data in enumerate(train_loader):            x, y = data            pred = net(x)            loss = loss_func(pred, y)            opt.zero_grad()            loss.backward()            opt.step()            batch_loss.append(loss.detach().numpy())        losses.append(np.mean(batch_loss))        if epoch % 100 == 0:            print("loss: ", loss)    plt.plot(np.arange(epochs), losses)      plt.show()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">net = model()loss_func = nn.CrossEntropyLoss()opt = torch.optim.SGD(net.parameters(), lr=0.05)epochs = 1000train_loader, test_loader = gen_data('iris.csv')train(net, loss_func, opt, epochs, train_loader)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>这里的损失函数，选择的是交叉熵损失函数，其自带sofmax层，可以串接在最后一层全连接输出后，达到分类效果</p></blockquote><p><img src="/posts/a174/image-20230110161720711.png" alt="训练损失"></p><h2 id="4-预测结果"><a href="#4-预测结果" class="headerlink" title="4.预测结果"></a>4.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(net, test_loader):    rights = 0    length = 0    for i, data in enumerate(test_loader):        x, y = data        pred = net(x)        rights += rightness(pred, y)[0]        length += rightness(pred, y)[1]        print('labels: ', y)        print('pred:   ', torch.max(pred, 1)[1], '\n')    print("acc: ", rights / length * 100)def rightness(pred, labels):    pred = torch.max(pred, 1)[1]    rights = pred.eq(labels).sum()    return rights, len(labels)prediction(net, test_loader)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/a174/image-20230110161742790.png" alt="预测结果"></p>]]></content>
      
      
      <categories>
          
          <category> softmax回归 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>共享单车预测</title>
      <link href="/posts/7f9c.html"/>
      <url>/posts/7f9c.html</url>
      
        <content type="html"><![CDATA[<h1 id="共享单车预测"><a href="#共享单车预测" class="headerlink" title="共享单车预测"></a>共享单车预测</h1><p>通过历史数据预测某一地区接下来一段时间内的共享单车的数量。数据保存在文件 bikes.csv 中，请按 11:1 的比例划分训练集和测试集，首先对数据进 行预处理，然后在训练集上训练，并在测试集上验证模型。设计神经网络数据进行拟合，利用训练后的模型对数据拟合并进行预测，记录误差，并绘制出拟合效果。</p><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport numpy as npimport torch.optim as optimimport matplotlib.pyplot as pltimport pandas as pd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>生成数据集</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">def gen_data(file):    data = pd.read_csv(file)    # one-hot编码    col_titles = ['season', 'weathersit', 'mnth', 'hr', 'weekday']    for i in col_titles:        # 生成形如season_0, season_1的列        dummies = pd.get_dummies(data[i], prefix=i)        data = pd.concat([data, dummies], axis=1)    # 删除列    col_titles_to_drop = ['instant', 'dteday', 'season', 'weathersit', 'weekday', 'mnth', 'workingday', 'hr']    data = data.drop(col_titles_to_drop, axis=1)        # 归一化    col_titles = ['cnt', 'temp', 'hum', 'windspeed']    for i in col_titles:        mean, std = data[i].mean(), data[i].std()        if i == 'cnt':            mean_cnt, std_cnt = mean, std        data[i] = (data[i] - mean) / std    print(len(data))    # 划分训练集和测试集11:1，`cnt`是label    train_data = data[: -len(data) // 12]    print(len(train_data))    train_value = train_data['cnt']    train_data = train_data.drop(['cnt'], axis=1).values    train_value = train_value.values.astype(float).reshape(-1, 1)    test_data = data[-len(data) // 12 : ]    print(len(test_data))    test_value = test_data['cnt']    test_data = test_data.drop(['cnt'], axis=1).values    test_value = test_value.values.astype(float).reshape(-1, 1)    return train_data, train_value, test_data, test_value, mean_cnt, std_cnt<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="2-构建模型"><a href="#2-构建模型" class="headerlink" title="2.构建模型"></a>2.构建模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">train_data, train_value, test_data, test_value, mean_cnt, std_cnt = gen_data('bikes.csv')input_size = train_data.shape[1] # 特征(列数)hidden_size = 10output_size = 1net = torch.nn.Sequential(torch.nn.Linear(input_size, hidden_size),                          torch.nn.Sigmoid(),                          torch.nn.Linear(hidden_size, output_size))<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(train_data, train_value, net, loss_func, opt, epochs):    losses = []    for i in range(epochs):        batch_loss = []        for start in range(0, len(train_data), batch_size):            if start + batch_size < len(train_data):                end = start + batch_size            else:                end = len(train_data)            x = torch.FloatTensor(train_data[start : end])            y = torch.FloatTensor(train_value[start : end])            pred = net(x)            loss = loss_func(pred, y)            opt.zero_grad()            loss.backward()            opt.step()            batch_loss.append(loss.detach().numpy())        if i % 100 == 0:            losses.append(np.mean(batch_loss))            print(i, np.mean(batch_loss))    plt.plot(np.arange(len(losses)) * 100, losses)    plt.show()loss_func = torch.nn.MSELoss()opt = torch.optim.SGD(net.parameters(), lr = 0.01)epochs = 1000batch_size = 128train(train_data, train_value, net, loss_func, opt, epochs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>这里没有用到加载器，DataLoader，而是手动划分了batch，并且能很好的对最后的批次进行处理(有可能不整除)</p></blockquote><p><img src="/posts/7f9c/image-20230110155947160-16760522453671.png" alt="训练损失"></p><h2 id="4-预测结果"><a href="#4-预测结果" class="headerlink" title="4.预测结果"></a>4.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(test_data, test_value, net, mean, std):    x = torch.FloatTensor(test_data)    y = torch.FloatTensor(test_value)    pred = net(x)    with torch.no_grad():        loss = loss_func(pred, y)    pred = pred.detach().numpy() * std + mean    y = y.detach().numpy() * std + mean    plt.plot(np.arange(x.shape[0]), y)    plt.plot(np.arange(x.shape[0]), pred, ':')    plt.show()    print("pred_loss: ", loss)prediction(test_data, test_value, net, mean_cnt, std_cnt)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>由于数据经过归一化，现在需要通过mean和std将数据还原回去</p></blockquote><p><img src="/posts/7f9c/image-20230110160002144.png" alt="预测结果"></p>]]></content>
      
      
      <categories>
          
          <category> 线性回归 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>神经网络拟合函数</title>
      <link href="/posts/5d68.html"/>
      <url>/posts/5d68.html</url>
      
        <content type="html"><![CDATA[<h1 id="神经网络拟合函数"><a href="#神经网络拟合函数" class="headerlink" title="神经网络拟合函数"></a>神经网络拟合函数</h1><h2 id="1-准备数据"><a href="#1-准备数据" class="headerlink" title="1.准备数据"></a>1.准备数据</h2><p>导入需要使用的第三方库</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchimport numpy as npimport randomimport matplotlib.pyplot as pltimport torch.nn as nnimport torch.functional as func<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>根据给定的<code>w、b</code>生成训练集和测试集(按9:1划分)</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">def gen_data(w, b, num):    x = torch.linspace(0, 10, num, dtype=torch.float32).reshape(-1, 1)    print(x.shape)    y = torch.matmul(x, w) + b    y += torch.normal(0, 0.01, y.shape)    print(y.shape)    train_x = x[: num * 9 // 10]    train_y = y[: num * 9 // 10]    test_x = x[num * 9 // 10 :]    test_y = y[num * 9 // 10 :]    return train_x, train_y, test_x, test_y<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p> 注意这里为什么要<code>reshape</code>，主要原因是要进行<code>矩阵乘法</code>，需要将向量转化成二维<code>张量</code>。</p></blockquote><pre class="line-numbers language-lang-python"><code class="language-lang-python">train_x, train_y, test_x, test_y = gen_data(torch.tensor([1.0]).reshape(1,1), 1.0, 100)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="2-建立模型"><a href="#2-建立模型" class="headerlink" title="2.建立模型"></a>2.建立模型</h2><p>拟合一次函数</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">class model(nn.Module):    def __init__(self):        super().__init__()        self.hidden = nn.Linear(1, 1)    def forward(self, x):        x = self.hidden(x)        return x<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>实现<code>forward</code>方法即可</p><h2 id="3-训练模型"><a href="#3-训练模型" class="headerlink" title="3.训练模型"></a>3.训练模型</h2><p>输入参数很简单，不用解释了</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">def train(train_x, train_y, epochs, opt, loss_func, net):    for epoch in range(epochs):        pred = net(train_x)        loss = loss_func(pred, train_y)        opt.zero_grad()        loss.backward()        opt.step()        if epoch % 500 == 0:            print("loss: ", loss)    return list(net.parameters())<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="4-设置超参数"><a href="#4-设置超参数" class="headerlink" title="4.设置超参数"></a>4.设置超参数</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">loss_func = torch.nn.MSELoss()opt = torch.optim.SGD(net.parameters(), lr = 0.01)epochs = 10000<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>这里选择均方误差损失函数，优化器选择随机梯度下降法，训练轮次为10000</p><h2 id="5-开始训练"><a href="#5-开始训练" class="headerlink" title="5.开始训练"></a>5.开始训练</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">net = model()train(train_x, train_y, epochs, opt, loss_func, net)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="6-预测结果"><a href="#6-预测结果" class="headerlink" title="6.预测结果"></a>6.预测结果</h2><pre class="line-numbers language-lang-python"><code class="language-lang-python">def prediction(test_x, test_y, net):    pred = net(test_x)    with torch.no_grad():        loss = loss_func(pred, test_y)    plt.plot(test_x.numpy(), test_y.numpy(), 'o')    plt.plot(test_x.numpy(), pred.detach().numpy())    plt.show()    print("pred_loss: ", loss)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-python"><code class="language-lang-python">prediction(test_x, test_y, net)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><img src="/posts/5d68/image-20230110003011174.png" alt="线性函数预测结果"></p><p>上图可以看出，误差已经很小了</p><h2 id="7-拟合三次多项式函数"><a href="#7-拟合三次多项式函数" class="headerlink" title="7.拟合三次多项式函数"></a>7.拟合三次多项式函数</h2><p>生成数据集</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">def gen_data(a, b, c, d, num):    x = torch.linspace(0, 1, num, dtype=torch.float32).reshape(-1, 1)    y = torch.matmul(x ** 3, a) + torch.matmul(x ** 2, b) + torch.matmul(x, c) + d    y += torch.normal(0, 0.01, y.shape)    plt.plot(x.numpy(), y.numpy(), 'o')    plt.show()    train_x = x[: num * 9 // 10]    train_y = y[: num * 9 // 10]    test_x = x[num * 9 // 10 :]    test_y = y[num * 9 // 10 :]    return train_x, train_y, test_x, test_y<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以利用<code>Sequential</code>创建神经网络</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">net = nn.Sequential(nn.Linear(1,5),                    nn.ReLU(),                    nn.Linear(5, 10),                    nn.ReLU(),                    nn.Linear(10, 5),                    nn.ReLU(),                    nn.Linear(5, 1)                   )<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>拟合结果如下，图一是对训练数据的拟合效果，图二是对测试数据的拟合效果</p><p><img src="/posts/5d68/image-20230110153658056.png" alt="训练数据的拟合效果"></p><p><img src="/posts/5d68/image-20230110153705079.png" alt="测试数据的拟合效果"></p><p>可以看出，结果应该是有一些<code>过拟合</code>的，可以通过一些优化方法来减少过拟合的影响</p>]]></content>
      
      
      <categories>
          
          <category> 线性回归 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>6.S081：5.xargs命令实现</title>
      <link href="/posts/852f.html"/>
      <url>/posts/852f.html</url>
      
        <content type="html"><![CDATA[<h1 id="6-S081：5-xargs命令实现"><a href="#6-S081：5-xargs命令实现" class="headerlink" title="6.S081：5.xargs命令实现"></a>6.S081：5.xargs命令实现</h1><h2 id="xargs-moderate"><a href="#xargs-moderate" class="headerlink" title="xargs (moderate)"></a>xargs (<a href="https://pdos.csail.mit.edu/6.828/2021/labs/guidance.html">moderate</a>)</h2><p>Write a simple version of the UNIX xargs program: read lines from the standard input and run a command for each line, supplying the line as arguments to the command. Your solution should be in the file <code>user/xargs.c</code>.</p><p>The following example illustrates xarg’s behavior:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ echo hello too | xargs echo byebye hello too$<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>Note that the command here is “echo bye” and the additional arguments are “hello too”, making the command “echo bye hello too”, which outputs “bye hello too”.</p><p>Please note that xargs on UNIX makes an optimization where it will feed more than argument to the command at a time. We don’t expect you to make this optimization. To make xargs on UNIX behave the way we want it to for this lab, please run it with the -n option set to 1. For instance</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ echo "1\n2" | xargs -n 1 echo lineline 1line 2$<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>Some hints:</p><ul><li>Use <code>fork</code> and <code>exec</code> to invoke the command on each line of input. Use <code>wait</code> in the parent to wait for the child to complete the command.</li><li>To read individual lines of input, read a character at a time until a newline (‘\n’) appears.</li><li>kernel/param.h declares MAXARG, which may be useful if you need to declare an argv array.</li><li>Add the program to <code>UPROGS</code> in Makefile.</li><li>Changes to the file system persist across runs of qemu; to get a clean file system run make clean and then make qemu.</li></ul><p>xargs, find, and grep combine well:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ find . b | xargs grep hello<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>will run “grep hello” on each file named b in the directories below “.”.</p><p>To test your solution for xargs, run the shell script xargstest.sh. Your solution is correct if it produces the following output:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ make qemu...init: starting sh$ sh < xargstest.sh$ $ $ $ $ $ hellohellohello$ $<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>You may have to go back and fix bugs in your find program. The output has many <script type="math/tex">` because the xv6 shell doesn't realize it is processing commands from a file instead of from the console, and prints a `</script> for each command in the file.</p><h2 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h2><p>需要用到管道命令，但不需要在<code>xargs</code>里面创建管道，只需从标准输出中读取前一条命令的结果即可，若又多个结果，则遇到一个换行符就停止读取。<code>xargs</code>后是<code>命令 参数</code>，显然需要父进程<code>fork</code>出一个子进程来执行后面的命令，并且将读取的结果附加到命令参数中。</p><p>值得注意的是，从标准输出中读取字符串时，本题推荐<code>一个字符顺序读取</code>(直接读取一个字符串，似乎会产生一些问题，暂时未解决)</p><p>然后就是一些字符串的简单处理，这里就直接放上代码了</p><h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><pre class="line-numbers language-lang-c"><code class="language-lang-c">#include "kernel/types.h"#include "user/user.h"#include "kernel/param.h"int main(int argc, char *argv[]){    char buf[MAXARG];    char *parse[MAXARG];    while(1){        memset(parse, 0, MAXARG);        for(int i = 0; i < argc; i++) parse[i] = argv[i];        int idx = argc, cnt = 0;        while(read(0, &buf[cnt], sizeof(char))){            if(buf[cnt] == '\n') break;            cnt++;        }        //printf("cnt = %d, buf = %s\n", cnt, buf);        if(cnt == 0) break;        buf[cnt] = '\0';        parse[idx++] = buf;        int i = 1;        while(i < strlen(buf)){            if(buf[i] == ' '){                buf[i] = '\0';                parse[idx++] = buf + i + 1;            }            i++;        }        int pid = fork();        if(pid == 0) exec(parse[1], parse + 1);        wait(0);    }    exit(0);}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> 6.S081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>6.S081：4.find功能实现</title>
      <link href="/posts/51d6.html"/>
      <url>/posts/51d6.html</url>
      
        <content type="html"><![CDATA[<h1 id="6-S081：4-find功能实现"><a href="#6-S081：4-find功能实现" class="headerlink" title="6.S081：4.find功能实现"></a>6.S081：4.find功能实现</h1><h2 id="find-moderate"><a href="#find-moderate" class="headerlink" title="find (moderate)"></a>find (<a href="https://pdos.csail.mit.edu/6.828/2021/labs/guidance.html">moderate</a>)</h2><p>Write a simple version of the UNIX find program: find all the files in a directory tree with a specific name. Your solution should be in the file <code>user/find.c</code>.</p><p>Some hints:</p><blockquote><ul><li>Look at <code>user/ls.c</code> to see how to read directories.</li><li>Use recursion to allow find to descend into sub-directories.</li><li>Don’t recurse into “.” and “..”.</li><li>Changes to the file system persist across runs of qemu; to get a clean file system run make clean and then make qemu.</li><li>You’ll need to use C strings. Have a look at K&amp;R (the C book), for example Section 5.5.</li><li>Note that == does not compare strings like in Python. Use strcmp() instead.</li><li>Add the program to <code>UPROGS</code> in Makefile.</li></ul></blockquote><p>Your solution is correct if produces the following output (when the file system contains the files <code>b</code> and <code>a/b</code>):</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ make qemu...init: starting sh$ echo > b$ mkdir a$ echo > a/b$ find . b./b./a/b    $<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h2><p>在一个特定目录下根据文件名查找，查看<code>user/ls.c</code>源码可知，需要先打开文件/目录，显然目录里面又包含文件，这样一来，这个查找显然是需要递归的，当查找到目录时，递归查找该目录下的文件，当遇到文件时返回即可。</p><p>特别注意的是，不能查找<code>.</code>和<code>..</code>这两个目录，不然废了。</p><p>在xv6中定义了三种类型：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#define T_DIR     1   // Directory#define T_FILE    2   // File#define T_DEVICE  3   // Devicestruct stat {  int dev;     // File system's disk device  uint ino;    // Inode number  short type;  // Type of file  short nlink; // Number of links to file  uint64 size; // Size of file in bytes};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>分别是目录、文件、设备，当xv6打开一个文件时，就要去申请一个<code>stat</code>数据结构，它包含了文件的信息</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">if((fd = open(path, 0)) < 0){    fprintf(2, "find cannot open %s\n", path);    return ;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-lang-c"><code class="language-lang-c">if(fstat(fd, &st) < 0){    fprintf(2, "find: cannot stat %s\n", path);    close(fd);    return;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>要读取当前目录的每个条目，需要</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">read(fd, &de, sizeof(de)) == sizeof(de)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><code>de</code>保存了文件名，可以通过<code>de.name</code>访问，之后与目标文件名称对比即可，可以用<code>strcmp</code></p><pre class="line-numbers language-lang-c"><code class="language-lang-c">strcmp(de.name, target);<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>做出本实验的关键是看懂xv6如何读取文件目录</p><h2 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h2><pre class="line-numbers language-lang-c"><code class="language-lang-c">#include "kernel/types.h"#include "kernel/stat.h"#include "user/user.h"#include "kernel/fs.h"void find(char *path, char *target){    char buf[512], *p;    int fd;    struct dirent de;    struct stat st;    if((fd = open(path, 0)) < 0){        fprintf(2, "find cannot open %s\n", path);        return ;    }    if(fstat(fd, &st) < 0){        fprintf(2, "find: cannot stat %s\n", path);        close(fd);        return;    }    if(strlen(path) + 1 + DIRSIZ + 1 > sizeof buf){        printf("ls: path too long\n");        close(fd);        return ;    }    if(st.type != T_DIR) {        close(fd);        return ;    }    strcpy(buf, path);    p = buf + strlen(buf);    *p++ = '/';    while(read(fd, &de, sizeof(de)) == sizeof(de)){        if(de.inum == 0 || strcmp(".", de.name) == 0 || strcmp("..", de.name) == 0)             continue;        int flag = strcmp(de.name, target);        memmove(p, de.name, DIRSIZ);        p[DIRSIZ] = 0;        //printf("%d %d\n", strlen(p), sizeof(p));        if(flag == 0) fprintf(1, "%s\n", buf);        find(buf, target);    }    close(fd);}int main(int argc, char *argv[]){    if(argc < 3){        fprintf(2, "argument counts error\n");        exit(0);    }    find(argv[1], argv[2]);    exit(0);}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> 6.S081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>6.S081：3.素数筛Primes实验</title>
      <link href="/posts/ac84.html"/>
      <url>/posts/ac84.html</url>
      
        <content type="html"><![CDATA[<h1 id="6-S081：3-素数筛Primes实验"><a href="#6-S081：3-素数筛Primes实验" class="headerlink" title="6.S081：3.素数筛Primes实验"></a>6.S081：3.素数筛Primes实验</h1><h2 id="Problem：primes-moderate-hard"><a href="#Problem：primes-moderate-hard" class="headerlink" title="Problem：primes (moderate)/(hard)"></a>Problem：primes (<a href="https://pdos.csail.mit.edu/6.828/2021/labs/guidance.html">moderate</a>)/(<a href="https://pdos.csail.mit.edu/6.828/2021/labs/guidance.html">hard</a>)</h2><blockquote><p>Write a concurrent version of prime sieve using pipes. This idea is due to Doug McIlroy, inventor of Unix pipes. The picture halfway down <a href="http://swtch.com/~rsc/thread/">this page</a> and the surrounding text explain how to do it. Your solution should be in the file <code>user/primes.c</code>.</p></blockquote><p>Your goal is to use <code>pipe</code> and <code>fork</code> to set up the pipeline. The first process feeds the numbers 2 through 35 into the pipeline. For each prime number, you will arrange to create one process that reads from its left neighbor over a pipe and writes to its right neighbor over another pipe. Since xv6 has limited number of file descriptors and processes, the first process can stop at 35.</p><p><img src="/posts/ac84/image-20220816184206364.png" alt="primes"></p><p>Some hints: </p><blockquote><ul><li>Be careful to close file descriptors that a process doesn’t need, because otherwise your program will run xv6 out of resources before the first process reaches 35.</li><li>Once the first process reaches 35, it should wait until the entire pipeline terminates, including all children, grandchildren, &amp;c. Thus the main primes process should only exit after all the output has been printed, and after all the other primes processes have exited.</li><li>Hint: <code>read</code> returns zero when the write-side of a pipe is closed.</li><li>It’s simplest to directly write 32-bit (4-byte) <code>int</code>s to the pipes, rather than using formatted ASCII I/O.</li><li>You should create the processes in the pipeline only as they are needed.</li><li>Add the program to <code>UPROGS</code> in Makefile.</li></ul></blockquote><p>Your solution is correct if it implements a pipe-based sieve and produces the following output:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">    $ make qemu    ...    init: starting sh    $ primes    prime 2    prime 3    prime 5    prime 7    prime 11    prime 13    prime 17    prime 19    prime 23    prime 29    prime 31    $<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h2><p>解决这道题的关键是让管道连续流动，父进程写数据，子进程读数据即可。那势必需要多次fork()，因为没遇到一个素数就需要创建一个新进程，管道流动起来。</p><p>首先明确，父进程的职能是将素数写入管道，子进程负责从管道读出素数，接着进行一次筛选，再创建新进程，此时子进程变成了父进程，重复上述操作即可，上述操作的伪代码可以这样描述。</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">while(condition){    if(fork() == 0){        read primes from pipe;        prime sieve;        print min prime;    }    else{        write primes into pipe;    }}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>只有fork()出来的父子进程才能通过一个管道通信，为了完成父进程写，子进程读，需要我们关闭父进程的读端和子进程的写端，管道通信的示意图如下</p><p><img src="/posts/ac84/image-20220816193304351.png" alt="pipe"></p><p>由于管道通信仅涉及父子进程，那么一个想法是在每一次创建进程之前，都先进行pipe(fd)，这样父子进程的fd都指向同一管道。</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">while(condition){    int fd[2];    pipe(fd);    if(fork() == 0){        close(fd[1]);        read(fd[0], buf, n);//read primes from pipe;        prime sieve;        print min prime;        exit(0);    }    else{        //write primes into pipe;        close(fd[0]);        write(fd[1], buf, n);        wait()        exit(0);    }}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这样基本上整个问题的大致思路就对了，只需要把细节考虑进去</p><ul><li>由于需要一组数在管道中流动，那么实际上需要传送一个数组，为了简单起见，每次只传送一个整数，这样就需要，一个循环来读写。</li><li>while停止的条件，可以记录每次传送数据的个数，当数据个数为0时，认为素数筛选完毕，此时退出循环，程序结束。</li><li>值得注意的是，由于每次循环都执行fork()操作，会导致进程指数级增长(多叉树)，我们期望的是这棵树长成一个单链表，那就需要wait()机制，<code>wait可以让父进程等待子进程的退出</code>，在父进程的代码里，加上wait()则会让父进程阻塞在这里，从而不会执行循环，也就不会形成多叉树。</li></ul><h2 id="彩蛋"><a href="#彩蛋" class="headerlink" title="彩蛋"></a>彩蛋</h2><p>其实没有wait()，让父进程直接退出也能达到不产生多叉树的效果，但是如果注释掉wait()，会发现一个意想不到的结果。</p><p>会发现，<code>命令提示符$</code>被打印了出来？这里的原因需要追溯到shell的运行机制，实际上shell也是一个c程序，中间也是一段fork()，子进程执行命令，父进程等待子进程的退出(显示命令提示符$)，由于cpu调度时，一次性让primes.c的主进程(shell的子进程)执行完了，进程退出后，会回到shell程序，接着打印出$，从而有了上述的结果。</p>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 6.S081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>6.S081：2.makefile初体验</title>
      <link href="/posts/ab96.html"/>
      <url>/posts/ab96.html</url>
      
        <content type="html"><![CDATA[<h1 id="6-S081：2-makefile初体验"><a href="#6-S081：2-makefile初体验" class="headerlink" title="6.S081：2.makefile初体验"></a>6.S081：2.makefile初体验</h1><h2 id="1-什么是makefile"><a href="#1-什么是makefile" class="headerlink" title="1.什么是makefile?"></a>1.什么是makefile?</h2><p>makefile能实现<code>自动编译</code>，对<code>多文件</code>的大工程项目，使用makefile可以事半功倍，可以省去许多<code>编译、链接</code>命令，在许多<code>IDE</code>中，通常不需要程序员使用命令行来编译源文件，而是点击一个编译运行按钮即可生成可执行文件，原因就是使用了makefile来进行自动编译</p><h2 id="2-一个简单的例子"><a href="#2-一个简单的例子" class="headerlink" title="2.一个简单的例子"></a>2.一个简单的例子</h2><p>对于一个C语言源程序<code>hello.c</code></p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#include <stdio.h>int main(){    printf("hello makefile!\n");    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>要想让这段程序能运行起来，需要经过以下几个步骤：</p><p><img src="/posts/ab96/image-20220722005008191.png" alt="编译"></p><ul><li>预处理</li><li>编译</li><li>汇编</li><li>链接</li></ul><p>对于单个C语言源文件可以以下编译命令来生成一个hello.out的可执行文件</p><pre class="line-numbers language-lang-makefile"><code class="language-lang-makefile">gcc -chello.c -o hello<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="3-一个复杂的例子"><a href="#3-一个复杂的例子" class="headerlink" title="3.一个复杂的例子"></a>3.一个复杂的例子</h2><p>对于多个C语言源程序</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">//----------f1.h------------#ifndef F1_H#define F1_Hvoid func1();#endif//----------f1.c------------#include <stdio.h>#inlcude "f1.h"void func1(){    printf("f1\n");}//----------f2.h------------#ifndef F2_H#define F2_Hvoid func2();#endif//----------f2.c------------#include <stdio.h>#inlcude "f2.h"void func2(){    printf("f2\n");}//----------main.c------------#include "f1.h"#include "f2.h"int main(){    func1();    func2();    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以看到<code>main.c</code>包含两个头文件<code>f1.h、f2.h</code>，要想的到一个可执行文件，需要先得到三个<code>可重定位目标文件</code>main.o、f1.o、f2.o，然后将三者<code>链接</code>起来生成可执行文件，具体来说分为以下几条命令</p><pre class="line-numbers language-lang-makefile"><code class="language-lang-makefile">gcc -c f1.c -o f1.ogcc -c f2.c -o f2.ogcc -c main.c -o main.ogcc main.o f1.o f2.o -o twoballs<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这样即可生成可执行文件twoballs，看着好像也不复杂，但是如果对f1.c进行修改，那么下次要运行twoballs程序时就要重新编译，再输入这四条命令，这样十分低效。</p><h2 id="4-编写一个简单的makefile"><a href="#4-编写一个简单的makefile" class="headerlink" title="4.编写一个简单的makefile"></a>4.编写一个简单的makefile</h2><p>makefile的语法格式如下：</p><pre class="line-numbers language-lang-makefile"><code class="language-lang-makefile">target: code code code     command<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>上一小节的例子写成makefile格式如下：</p><pre class="line-numbers language-lang-makefile"><code class="language-lang-makefile">twoballs: main.o f1.o f2.o    gcc main.o f1.o f2.o -o twoballsmain.o: main.c    gcc -c main.c -o main.of1.o: f1.c    gcc -c f1.c -o f1.of2.o: f2.c    gcc -c f2.c -o f2.oclean:    rm twoballs main.o f1.o f2.o -rf<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>输入<code>make</code>，即可生成twoballs</p><h2 id="5-编写一个有逼格的makefile"><a href="#5-编写一个有逼格的makefile" class="headerlink" title="5.编写一个有逼格的makefile"></a>5.编写一个有逼格的makefile</h2><p>我们将上一节的makefile再修改一下，显得专业一点，让别人看不懂</p><pre class="line-numbers language-lang-makefile"><code class="language-lang-makefile">#定义，引用定义时，格式为：$(VAR)OBJ = main.o f1.o f2.oCC = gccRM = rm -rfALL = twoballs# $^表示上一行的所有依赖， $@表示目标文件$(ALL): $(OBJ)    $(CC) $^ -o $@%.o: %.c    $(CC) -c $^ -o $@clean: $(OBJ) $(ALL)    $(RM) $^<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这样一个有逼格的makefile就写好啦~</p>]]></content>
      
      
      <categories>
          
          <category> 编译 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 6.S081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>6.S081：1.关于fork()的思考</title>
      <link href="/posts/undefined.html"/>
      <url>/posts/undefined.html</url>
      
        <content type="html"><![CDATA[<h1 id="6-S081：1-关于fork-的思考"><a href="#6-S081：1-关于fork-的思考" class="headerlink" title="6.S081：1.关于fork()的思考"></a>6.S081：1.关于fork()的思考</h1><h2 id="1-fork"><a href="#1-fork" class="headerlink" title="1.fork"></a>1.fork</h2><pre class="line-numbers language-lang-c"><code class="language-lang-c">int pid = fork();  //创建一个进程，返回子进程的PID<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="1-1fork用法"><a href="#1-1fork用法" class="headerlink" title="1.1fork用法"></a>1.1fork用法</h3><p>一个进程可以使用<code>fork()</code>系统调用创建一个新的进程(称为子进程)，fork()创建了一个<code>新的进程</code>，其<code>内存</code>与父进程完全一样，fork在子进程中返回<code>0</code>，父进程中返回<code>子进程的PID</code>。</p><h3 id="1-2fork疑云"><a href="#1-2fork疑云" class="headerlink" title="1.2fork疑云"></a>1.2fork疑云</h3><p>且看一个例子：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#include <unistd.h>#include <stdio.h>int main(){    int count = 0;    int pid = fork();    if (pid < 0)        printf("fork error\n");    else if(pid == 0)        count++, printf("pid = %d count_ad = %p : %d\n", getpid(), &count, count);    else        count++, printf("pid = %d count_ad = %p : %d\n", getpid(), &count, count);    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>输出如下：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">pid = 240 count_ad = 0x7ffdb7979330 : 1pid = 241 count_ad = 0x7ffdb7979330 : 1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>这里会产生一个疑问，既然两个进程并<code>未共享count</code>变量，那么对count自增操作自然是分别<code>独立</code>进行，也就是最终结果都是count = 1，但count的<code>地址</code>却相同，纠结的点在于如果变量的地址相同，那第二次执行自增的count = 2</p><p>问题解决：</p><blockquote><p>由于fork()系统调用会产生一个新的进程，而在每个进程眼中，自己都<code>独享整个内存地址空间</code>，也就是说，child-&gt;count_ad虽然和parent-&gt;count_ad<code>逻辑地址相同</code>，但其实际<code>物理地址不同</code>。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 6.S081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Sysprog：1. Shell</title>
      <link href="/posts/sfaaf.html"/>
      <url>/posts/sfaaf.html</url>
      
        <content type="html"><![CDATA[<h1 id="lab1：Shell"><a href="#lab1：Shell" class="headerlink" title="lab1：Shell"></a>lab1：Shell</h1><h2 id="Question-1"><a href="#Question-1" class="headerlink" title="Question 1:"></a><strong>Question 1</strong>:</h2><p>If you add a background command after the pipeline command, can the pipeline work?</p><h3 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h3><p><img src="/posts/sfaaf/image-20221004234233382.png" alt="Q1"></p><p>由上图可知，background command会导致pipeline不能正常工作，shell提前打印出了<code>ccs-lab@</code>字样，并且在执行完background command后依然在等待用户输入，我实现的background如下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_BACK: {    struct Cmd_Back *t = (struct Cmd_Back *) cmd;    Cmd_t back = t->back;    if(fork() == 0){          Cmd_run(back);     }      //wait(0);  //no care child     break;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>而在pipe的实现机制下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_PIPE: {    struct Cmd_Pipe *t = (struct Cmd_Pipe *) cmd;    Cmd_t left = t->left;    Cmd_t right = t->right;    int fd[2];    pipe(fd);    if(fork() == 0) {           close(1);           dup(fd[1]);        close(fd[0]);           close(fd[1]);           Cmd_run(left);    }    wait(0);    if(fork() == 0){          close(0);           dup(fd[0]);        close(fd[0]);        close(fd[1]);         Cmd_run(right);       }    close(fd[0]);    close(fd[1]);    wait(0);    break;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>分析其原因，执行right节点命令时，fork出子进程来执行<code>grep</code>，由于此时执行的是background command，父进程并未等待子进程退出(<code>grep程序还在运行</code>)，可以直接返回到父进程，最终返回到main函数中，此时进入新一轮循环，<code>css-lab@</code>被打印出来了，但此时cpu又调度了grep进程，grep执行完后，又返回到main里，这时shell会等待用户输入命令，所以出现了一个空行，等待命令输入</p><h2 id="Question-2"><a href="#Question-2" class="headerlink" title="Question 2:"></a><strong>Question 2</strong>:</h2><p>Why <code>fork</code> and <code>exec</code> are not combined in a single call?</p><h3 id="Solution-1"><a href="#Solution-1" class="headerlink" title="Solution"></a>Solution</h3><p><code>fork</code>和<code>exec</code>分离，可以使得在fork后exec前，设置一些必要的参数，如设置argv[]参数、打开或关闭文件描述符</p><p>例如在实现<code>redirect</code>过程中，执行<code>left节点</code>命令之前，先关闭<code>stdout/stdin</code>，再打开要重定向的文件，然后直接left节点命令，即可实现重定向</p><h2 id="Question-3"><a href="#Question-3" class="headerlink" title="Question 3:"></a><strong>Question 3</strong>:</h2><p>What are the advantages of pipes over temporary files in this situation?</p><ul><li>use pipe</li></ul><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">echo hello world | wc<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>without pipe</li></ul><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">echo hello world > /tmp/xyz ; wc < /tmp/xyz<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="Solution-2"><a href="#Solution-2" class="headerlink" title="Solution"></a>Solution</h3><p>代码简洁明了，不需要暂时文件来存储中间信息，便于编写组合命令，实现复杂命令</p><h2 id="Exercise-1"><a href="#Exercise-1" class="headerlink" title="Exercise 1:"></a><strong>Exercise 1</strong>:</h2><p>Please complete the corresponding part in:</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">Cmd_t Cmd_Back_new(Cmd_t back);Cmd_t Cmd_Pipe_new(Cmd_t left, Cmd_t right);Cmd_t Cmd_Redir_new(Cmd_t left, Cmd_t right, int fd);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>Hint: you can refer to the data structure defined in the <code>ast.h</code>.</p><h3 id="Soution"><a href="#Soution" class="headerlink" title="Soution"></a>Soution</h3><pre class="line-numbers language-lang-c"><code class="language-lang-c">Cmd_t Cmd_Back_new(Cmd_t back){    Cmd_Back cmd;    NEW(cmd);    cmd->type = CMD_BACK;    cmd->back = back;    return (Cmd_t)cmd;}Cmd_t Cmd_Pipe_new(Cmd_t left, Cmd_t right){    Cmd_Pipe cmd;    NEW(cmd);    cmd->type = CMD_PIPE;    cmd->left = left;    cmd->right = right;    return (Cmd_t)cmd;}Cmd_t Cmd_Redir_new(Cmd_t left, Cmd_t right, int fd){    Cmd_Redir cmd;    NEW(cmd);    cmd->type = CMD_REDIR;    cmd->left = left;    cmd->right = right;    cmd->fd = fd;    return (Cmd_t)cmd;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="Exercise-2"><a href="#Exercise-2" class="headerlink" title="Exercise 2:"></a><strong>Exercise 2</strong>:</h2><p>Please complete the <code>void Cmd_print(Cmd_t cmd)</code>. Then call <code>void Cmd_print(Cmd_t cmd)</code> in <code>main.c</code> and comment out <code>Cmd_run(root)</code>.</p><p>Finally, compile and run the project in Clion and use the following test cases to check whether your answer is correct.</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">cat hello.txt | grep -n hello > c.txt ; ls -l<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>The result will be as shown in the figure below.</p><p><img src="/posts/sfaaf/print1.PNG" alt="img"></p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash"> sleep 10 &<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>The result will be as shown in the figure below.</p><p><img src="/posts/sfaaf/image-20220924143217317.png" alt="img"></p><h3 id="Solution-3"><a href="#Solution-3" class="headerlink" title="Solution"></a>Solution</h3><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_BACK: {    struct Cmd_Back *t = (struct Cmd_Back *) cmd;    Cmd_t back = t->back;    Cmd_print(back);    printf("& ");    break;}case CMD_PIPE: {    struct Cmd_Pipe *t = (struct Cmd_Pipe *) cmd;    Cmd_t left = t->left;    Cmd_t right = t->right;    Cmd_print(left);    printf("| ");    Cmd_print(right);    break;}case CMD_REDIR: {    struct Cmd_Redir *t = (struct Cmd_Redir *) cmd;    Cmd_t left = t->left;    Cmd_t right = t->right;    Cmd_print(left);    printf("> ");    Cmd_print(right);    break;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>运行效果如下：</p><p><img src="/posts/sfaaf/image-20221004233932251.png" alt="Q2"></p><h2 id="Exercise-3"><a href="#Exercise-3" class="headerlink" title="Exercise 3:"></a><strong>Exercise 3</strong>:</h2><p>Please complete the <code>void Cmd_run(Cmd_t cmd)</code>. Regarding redirection commands, you only need to implement output redirection. Commands like this:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">cat hello.txt > c.txt<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>Then call <code>void Cmd_run(Cmd_t cmd)</code> in <code>main.c</code> and comment out <code>Cmd_print(root)</code>.</p><p>Finally, compile and run the project in Clion and use the following test cases to check whether your answer is correct.</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">cat hello.txt | grep -n hello > c.txt ; cat c.txt ; ls<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>Before entering the command, create a file called <code>hello.txt</code> in the current working directory. The content of the file is as follows:</p><p><img src="/posts/sfaaf/contents.PNG" alt="img"></p><p>The result will be as shown in the figure below.</p><p><img src="https://csslab-ustc.github.io/courses/sysprog/labs/lab1/figures/test1.PNG" alt="img"></p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">sleep 10 &<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>If you enter the command <code>sleep 10</code>, you cannot use the shell for ten seconds. However, when you add an extra <code>&amp;</code> to your command, you’ll be able to use the shell immediately. The result will be as shown in the figure below.</p><p><img src="/posts/sfaaf/test2.PNG" alt="img"></p><h3 id="Thinking"><a href="#Thinking" class="headerlink" title="Thinking"></a>Thinking</h3><p>BACK设计思路：</p><ul><li>父进程不等候子进程的退出</li></ul><p>PIPE设计思想：</p><ul><li>先fork一个子进程用来运行left，使得文件描述符1指向管道的写端，当left执行时会输出到管道里，第二次fork，子进程也会指向这个管道，让文件描述符0指向管道的读端，当right执行时会从管道中读入，最后父进程关闭管道</li></ul><p>REDIR设计思想：</p><ul><li>获取right的文件名，关闭文件描述符0/1，然后再打开right文件，运行left即可</li></ul><h3 id="Solution-4"><a href="#Solution-4" class="headerlink" title="Solution"></a>Solution</h3><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_BACK: {    struct Cmd_Back *t = (struct Cmd_Back *) cmd;    Cmd_t back = t->back;    if(fork() == 0){        Cmd_run(back);    }    //wait(0);        // don't care child    break;}case CMD_PIPE: {    struct Cmd_Pipe *t = (struct Cmd_Pipe *) cmd;    Cmd_t left = t->left;    Cmd_t right = t->right;    int fd[2];    pipe(fd);    if(fork() == 0) {    // run left        close(1);        dup(fd[1]);        close(fd[0]);        close(fd[1]);        Cmd_run(left);    }    wait(0);    if(fork() == 0){    // run right        close(0);        dup(fd[0]);        close(fd[0]);        close(fd[1]);        Cmd_run(right);    }    close(fd[0]);    close(fd[1]);    wait(0);    break;}case CMD_REDIR: {    struct Cmd_Redir *t = (struct Cmd_Redir *) cmd;    Cmd_t left = t->left;    Cmd_t right = t->right;    Cmd_Atom redir = (Cmd_Atom) right;     // get redir filename    char *filename;    struct node *node = redir->node;    filename = node->data;    close(t->fd);  // close fd 0/1    open(filename, O_CREAT | O_RDWR, 0777); // new file obtain fd 0/1    Cmd_run(left);    break;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>运行结果如下：</p><p><img src="/posts/sfaaf/image-20221004234347458.png" alt="Q3"></p><h2 id="Challenge"><a href="#Challenge" class="headerlink" title="Challenge:"></a>Challenge:</h2><p>Implement the input redirection.</p><h3 id="Solution-5"><a href="#Solution-5" class="headerlink" title="Solution"></a>Solution</h3><ul><li>修改<code>parser.y</code></li></ul><pre class="line-numbers language-lang-c"><code class="language-lang-c">%left '|' ';'%left '>'%left '&'%left '<'%%line            :  command '\n'                { root = $1; $$ = 0; return 0; }                | '\n'               { root = 0; $$ = 0; return 0; }                ;command            :  basic_command            { $$ = $1;}                |  command ';' command      { $$ = Cmd_Seq_new($1, $3);}                |  command '&'                { $$ = Cmd_Back_new($1);}                |  command '|' command         { $$ = Cmd_Pipe_new($1, $3);}                |  command '>' command        { $$ = Cmd_Redir_new($1, $3, 1);}                |  command '<' command        { $$ = Cmd_Redir_new($1, $3, 0);}                ;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>修改<code>scanner.l</code></li></ul><pre class="line-numbers language-lang-c"><code class="language-lang-c"><<EOF>>                                 {return 0;}"&"|";"|"|"                             {return yytext[0];}">"                                     {return yytext[0];}"<"                                     {return yytext[0];}"\n"                                    {return yytext[0]; }[ ]*                                    {}{ID}+                                    {yylval = strdup(yytext); return T_ARG; }.                                        {printf("lex err:[%s]", yytext); return 0; }<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>原<code>Redir</code>代码无需修改，因为<code>yyparse</code>会根据<code>&lt;</code>和<code>&gt;</code>调用不同的构造函数，区别在于传入的<code>fd</code>不同</li></ul><pre class="line-numbers language-lang-c"><code class="language-lang-c">(yyval.cmd) = Cmd_Redir_new((yyvsp[-2].cmd), (yyvsp[0].cmd), 1);(yyval.cmd) = Cmd_Redir_new((yyvsp[-2].cmd), (yyvsp[0].cmd), 0);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>编译运行即可，效果如下：</p><p><img src="/posts/sfaaf/image-20221004234434766-16669737783612.png" alt="challenge"></p>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Sysprog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Sysprog：2. mini Network-Analyzer</title>
      <link href="/posts/asfdh.html"/>
      <url>/posts/asfdh.html</url>
      
        <content type="html"><![CDATA[<h1 id="mini-Network-Analyzer"><a href="#mini-Network-Analyzer" class="headerlink" title="mini Network-Analyzer"></a>mini Network-Analyzer</h1><h2 id="Part-D：packet-spoofing"><a href="#Part-D：packet-spoofing" class="headerlink" title="Part D：packet spoofing"></a>Part D：packet spoofing</h2><p>实验结果：前8个包</p><p><img src="/posts/asfdh/image-20221027235610817.png" alt="Forge"></p><p>文件结构</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">|-- Forge    |-- method.h    |-- method.c    |-- UDP_forge.h    |-- UDP_forge.c    |-- TCP_forge.h    |-- TCP_forge.c    |-- ARP_forge.h    |-- ARP_forge.c    |-- ICMP_forge.h    |-- ICMP_forge.c    |-- main<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>利用Raw Socket可以实现发送用户自定义的数据包,在实现各种forgery之前，先定义一个工具文件<code>method.h/method.c</code>，实现公共部分，定义的接口如下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">void get_eth_index(int sock_raw);void get_mac(int sock_raw, uint16_t protocol, int *total_len);void str2ip(char *src, unsigned char *dst);unsigned short checksum(unsigned char *buf, int size);void forge(void(*pro)(int *total_len), uint16_t mac_protocol, uint8_t ip_protocol);     // forge templatevoid send_ip(void(*func)(int *total_len), int sock_raw, uint8_t protocol, int *total_len);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在forge函数中，通过传入相应的协议函数指针，即可生成一个对应协议的数据包，并且由于大多数网络层以上的协议都是基于IP的，故将<code>send_ip</code>也写成一个模板。</p><p>例如要发送TCP，参数为：<code>1：填充协议的函数指针；2：MAC的协议字段；3：IP的协议字段(ARP协议此参数填0即可)</code></p><pre class="line-numbers language-lang-c"><code class="language-lang-c">forge(send_tcp, ETH_P_IP, IPPROTO_TCP);<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其中<code>forge()</code>实现如下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">void forge(void(*pro)(int *total_len), uint16_t mac_protocol, uint8_t ip_protocol){    int sock_raw = sock_raw = socket(AF_PACKET,SOCK_RAW,ETH_P_IP);    if(sock_raw == -1)        printf("error in socket\n");    send_buff = (unsigned char*)malloc(64);             // increase in case of large data.    memset(send_buff, 0, 64);    int total_len = 0;    get_eth_index(sock_raw);                // interface number    get_mac(sock_raw, mac_protocol, &total_len);    if(mac_protocol == ETH_P_ARP)  pro(&total_len);    else send_ip(pro, sock_raw, ip_protocol, &total_len);    struct sockaddr_ll sadr_ll;    sadr_ll.sll_ifindex = ifreq_idx.ifr_ifindex;    sadr_ll.sll_halen   = ETH_ALEN;    for(int i = 0; i < 6; i++)        sadr_ll.sll_addr[i]  = dst_mac[i];    int cnt = 1;    printf("sending...\n");    while(cnt-- > 0){        int send_len = sendto(sock_raw, send_buff, 64, 0, (const struct sockaddr*)&sadr_ll, sizeof(struct sockaddr_ll));        if(send_len < 0){            printf("error in sending....sendlen = %d....errno = %d\n", send_len, errno);            break;        }    }    close(sock_raw);}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="1-UDP-packet-forgery"><a href="#1-UDP-packet-forgery" class="headerlink" title="1.UDP packet forgery"></a>1.UDP packet forgery</h3><pre class="line-numbers language-lang-c"><code class="language-lang-c">void send_udp(int *total_len){    printf("udp packaging start ... \n");    struct udphdr *uh = (struct udphdr *)(send_buff + sizeof(struct iphdr) + sizeof(struct ethhdr));    uh->source    = htons(23451);    uh->dest    = htons(23452);    uh->check    = 0;   //Many OSes ignore this field , so we do not calculate it.    *total_len += sizeof(struct udphdr);    uh->len        = htons((*total_len - sizeof(struct iphdr) - sizeof(struct ethhdr)));    printf("udp packaging start ... \n");}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="2-TCP-packet-forgery"><a href="#2-TCP-packet-forgery" class="headerlink" title="2.TCP packet forgery"></a>2.TCP packet forgery</h3><p>由于TCP的checksum需要添加伪首部才能计算正确，故在头文件定义伪首部</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">struct psd_hdr{    unsigned int src_ipaddr;    unsigned int dst_ipaddr;    unsigned char nop;    unsigned char protocol;    unsigned short tcp_len;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><code>TCP_forge.c</code>如下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">void send_tcp(int *total_len){    struct tcphdr *th = (struct tcphdr *)(send_buff + sizeof(struct iphdr) + sizeof(struct ethhdr));    printf("tcp packaging start ... \n");    //write udp_header    th->th_sport    = htons(2333);    th->th_dport    = htons(80);    th->th_seq      = htons(0);    th->th_ack      = htons(0);    th->th_off      = sizeof (struct tcphdr) / 4;    th->th_flags    = TH_SYN;    th->th_win      = htons(4096);    // define pseudo header for tcp checksum    struct psd_hdr psd;    psd.src_ipaddr = inet_addr(src_ip);    psd.dst_ipaddr = inet_addr(dst_ip);    psd.nop = 0;    psd.protocol = 6;    psd.tcp_len = htons(sizeof (struct tcphdr));    unsigned char cur[1000];    memcpy(cur, &psd, sizeof(struct psd_hdr));    memcpy(cur + sizeof (struct psd_hdr), th, sizeof(struct tcphdr));    th->th_sum = htons(checksum(cur, sizeof(struct tcphdr) + sizeof (struct psd_hdr)));    *total_len += sizeof(struct tcphdr);    printf("tcp packaging done ... \n");}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="3-ICMP-packet-forgery"><a href="#3-ICMP-packet-forgery" class="headerlink" title="3.ICMP packet forgery"></a>3.ICMP packet forgery</h3><p>由于struct icmp中含有union，为了方便起见，定义宏</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">// define icmp union#define icmp_id         icmp_hun.ih_idseq.icd_id#define icmp_seq        icmp_hun.ih_idseq.icd_seq<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><code>ICMP_forge.c</code>实现如下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">void send_icmp(int *total_len){    printf("icmp packaging start ... \n");    struct icmp *ih = (struct icmp *)(send_buff + sizeof(struct iphdr) + sizeof(struct ethhdr));    ih->icmp_type = ICMP_ECHO;    ih->icmp_code = 0;    ih->icmp_cksum = htons(checksum((unsigned char *)(send_buff + sizeof (struct ethhdr) + sizeof (struct iphdr)), sizeof (struct icmp)));    ih->icmp_id = 0;    ih->icmp_seq = 0;    *total_len += sizeof(struct icmp);    printf("icmp packaging done ... \n");}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="4-ARP-packet-forgery"><a href="#4-ARP-packet-forgery" class="headerlink" title="4.ARP packet forgery"></a>4.ARP packet forgery</h3><p>由于<code>ARP</code>与<code>IP</code>一样都是网络层协议，故在<code>MAC</code>层需要将<code>protocol</code>改为<code>ETH_P_ARP</code>。</p><p>并且ARP协议的头部定义需要在 <code>&lt;netinet/if_ether.h&gt;</code>头文件下找，而非<code>&lt;net/if_arp.h&gt;</code></p><p><code>ARP_forge.c</code>定义如下</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">void send_arp(int *total_len){    printf("arp packaging start ... \n");    struct ether_arp *ah = (struct ether_arp *)(send_buff + sizeof (struct ethhdr));    str2ip(src_ip, ah->arp_spa);    str2ip(dst_ip, ah->arp_tpa);    for(int i = 0; i < 6; i++){        ah->arp_sha[i] = (unsigned char)(ifreq_mac.ifr_hwaddr.sa_data[i]);        ah->arp_tha[i]   =  (unsigned char)0x00;    }    ah->ea_hdr.ar_hrd = htons(0x01);    ah->ea_hdr.ar_pro = htons(ETH_P_IP);    ah->ea_hdr.ar_hln = ETH_ALEN;    ah->ea_hdr.ar_pln = 0x04;    ah->ea_hdr.ar_op = htons(ARPOP_REQUEST);    *total_len += sizeof (struct ether_arp);    printf("arp packaging done ... \n");}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="Part-E：Design-your-own-MAC-layer-protocol"><a href="#Part-E：Design-your-own-MAC-layer-protocol" class="headerlink" title="Part E：Design your own MAC layer protocol"></a>Part E：Design your own MAC layer protocol</h2><p>文件结构：两个主机源代码大部分相同</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">|-- final    |-- protocol.h    // define struct own_hdr and interface for send or receive    |-- send.c    |-- receive.c    |-- main.c<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>自定义了一个以太网头部格式如下，头部后面即要发送的数据</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">struct own_hdr{    unsigned char dst_mac[6];    unsigned char src_mac[6];};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>分别定义send.c/receive.c用来实现发送和接收的方法</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">// sendvoid get_eth_index();void get_own_mac(unsigned char *buffer, unsigned char dst_mac[]);void send_data(unsigned char *buffer);// receivevoid packet_print(unsigned char* buffer, int buffer_len);void own_mac_header(unsigned char *buffer);bool filterByMacAddress(unsigned char *buffer, unsigned char mac_addr[], int type);bool equal(unsigned char *dst, unsigned char *src);void get_data(unsigned char *buffer);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在主函数中，创建两个raw socket分别用来发送和接收数据包，主机A先发送，未收到主机B的回复之前不会再次发送，主机B同理。运行结果如下</p><p><img src="/posts/asfdh/image-20221021181821595.png" alt="own protocol"></p>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Sysprog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Sysprog：3. A Minimal Debugger</title>
      <link href="/posts/1234.html"/>
      <url>/posts/1234.html</url>
      
        <content type="html"><![CDATA[<h1 id="lab3-：A-Minimal-Debugger"><a href="#lab3-：A-Minimal-Debugger" class="headerlink" title="lab3 ：A Minimal Debugger"></a>lab3 ：A Minimal Debugger</h1><h2 id="Exercise-1"><a href="#Exercise-1" class="headerlink" title="Exercise 1:"></a>Exercise 1:</h2><p>Now read the source file and answer the following questions.</p><ul><li>How the <code>regs</code> command is implemented?</li></ul><h3 id="Solution"><a href="#Solution" class="headerlink" title="Solution"></a>Solution</h3><ul><li>定义<code>user_regs_struct</code>类型变量<code>regs</code>接收寄存器组</li><li><p>调用<code>ptrace (PTRACE_GETREGS, c-&gt;pid, 0, &amp;regs)</code>将寄存器的值写入<code>regs</code></p></li><li><p>打印各寄存器的值</p></li></ul><h2 id="Exercise-2"><a href="#Exercise-2" class="headerlink" title="Exercise 2:"></a>Exercise 2:</h2><p>Now, how the breaking point command <code>b 0xaddr</code> is implemented? In fact, there is a serious bug in current implementation we offered you. Let’s check where is this bug. First run this</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">objdump -d hello<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>and figure out the address of the function <code>print</code>, suppose that address is <code>0xaddr</code> on your machine. Now in the <code>ssedb</code>, you set up a break point on address <code>0xaddr</code> by typing (remember this address must be in hexadecimal, which has a leading <code>0x</code>):</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">(ssedb) b 0xaddr<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>now peek the registers:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">(ssedb) regs<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>What’s the value of <code>rip</code>? Is this value right? Why?</p><p>And then disassembly the content at address <code>0xaddr</code>.</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">(ssedb) x/x 0xaddr<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>What’s there? Have you detected the bug? How to fix this bug?</p><h3 id="Solution-1"><a href="#Solution-1" class="headerlink" title="Solution"></a>Solution</h3><ol><li><p><code>rip</code>的值需要从两方面讨论，因为<code>rip</code>存储的是下一条指令的地址，设置断点时，将第一个字节设置为<code>0xcc</code>即<code>int 3</code>软中断指令，当子进程执行到此处时，会停止且发送信号给父进程，由于已经执行了<code>int 3</code> 那么其实<code>rip</code>的值是正确的，因为它确实指向了<code>int 3</code>的下一条指令的地址，但是，这种情况却不是我们想要的，因为我们为了让子进程停在断点处，于是覆盖了第一个字节，那么第一个字节的指令并未执行，所以在父进程中我们就需要恢复断点信息(因为第一个字节的指令并未执行)，即</p><ul><li><code>rip = rip - 1</code></li><li>将<code>0xcc</code>改为原来的值</li></ul><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_KIND_BREAK:{    unsigned long brk = 0x00000000000000cc;    unsigned long data;    unsigned long back;     // copy the origin data    data = ptrace(PTRACE_PEEKDATA, c->pid, c->u.addr, 0);    back = data;    printf ("init data     = %lx\n", data);    data = (data & 0xffffffffffffff00) | brk;    printf ("revised data  = %lx\n", data);    ptrace(PTRACE_POKEDATA, c->pid, c->u.addr, data);    // child will stop in (int 3)    ptrace(PTRACE_CONT, c->pid, 0, 0);    waitChild ();    // write back the origin rip    struct user_regs_struct regs;    ptrace (PTRACE_GETREGS, c->pid, 0, &regs);    regs.rip--;    ptrace (PTRACE_SETREGS, c->pid, 0, &regs);    // write back the origin data    ptrace(PTRACE_POKEDATA, c->pid, c->u.addr, back);    return;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>没有显示前导0，只需在打印时指定正确的格式即可，即<code>printf ("%lx: %016lx\n", addr, data)</code></p><p><img src="/posts/1234/image-20221026221903731.png" alt="Q2"></p></li></ol><p>可以看到最低字节又修改为原来的值了</p><h3 id="improvement"><a href="#improvement" class="headerlink" title="improvement"></a>improvement</h3><p>由于所给代码在<code>b addr</code>后会接着执行一条<code>ptrace(PTRACE_CONT, c-&gt;pid, 0, 0);</code>，这会导致<code>traced</code>程序直接命中断点，故将<code>CONT</code>指令移除，只设置断点，即写入一条<code>int 3</code>指令，并将原数据保存起来，定义<code>struct breakpoint</code></p><pre class="line-numbers language-lang-c"><code class="language-lang-c">struct breakpoint{    long addr;    unsigned long origin;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>存放其地址和原始数据，而真正命中断点后还原断点信息的代码放在<code>case CMD_KIND_CONT</code></p><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_KIND_CONT:{    ptrace(PTRACE_CONT, c->pid, 0, 0);    int status;    wait (&status);    // back to origin rip    struct user_regs_struct regs;    ptrace (PTRACE_GETREGS, c->pid, 0, &regs);    unsigned long long rip = regs.rip - 1;    unsigned long data = ptrace(PTRACE_PEEKDATA, c->pid, rip, 0);    if((uint8_t)data == 0xcc) {      // meet break point        regs.rip--;        ptrace(PTRACE_SETREGS, c->pid, 0, &regs);        // write back the origin data        unsigned long origin = get_origin();        ptrace(PTRACE_POKEDATA, c->pid, rip, origin);        printf("meet the break point %d at 0x%016x\n", idx, rip);    }    if(WIFEXITED(status))    // exit ?        printf("process %d is exit!\n", c->pid);    return;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这样做的结果是，<code>b addr</code>只设置断点，不会让<code>traced</code>程序继续运行，而<code>c</code>可以<code>traced</code>程序运行，并且可以命中断点，由于已经有了断点信息，索性增加了一个新的指令<code>info</code>：打印所有<code>break point</code>，实现如下：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">int len = 0;    // 断点个数int idx = 0;    // 命中个数struct breakpoint bp[100];void update_breakpoint_table(char *name, long addr, unsigned long origin){    bp[len].name = name;    bp[len].addr = addr;    bp[len].origin = origin;    len++;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在<code>execCommand</code>中新增<code>case</code></p><pre class="line-numbers language-lang-c"><code class="language-lang-c">case CMD_KIND_INFO:{    for(int i = 0; i < len; i++)        printf("break point %d  :  0x%016x\n", i + 1, bp[i].addr);    return ;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>最终执行的效果:</p><p><img src="/posts/1234/image-20221029235603319.png" alt="improvement"></p><h2 id="Exercise-3"><a href="#Exercise-3" class="headerlink" title="Exercise 3:"></a>Exercise 3:</h2><p>There is also a command to disassembly bianry into assembly intructions, but has not be completed. Now run</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">(ssedb) x/i 0xaddr<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>you’ll see an error message indicating the file position you should supply code. Implement it. (Hint: manual disassemblying is tedious and error-prone, so you may find some libraries are helpful, such as the <a href="https://github.com/zyantific/zydis">zydis</a>.)</p><h3 id="Solution-2"><a href="#Solution-2" class="headerlink" title="Solution"></a>Solution</h3><p>使用第三方库<code>zydis</code>，指令在我的机器上按<code>little endian</code>存储，故需要将获得的8字节16进制数据逆序且两两一组存放</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">ZyanU8 arr[80];  // define byte arrlong addr = c->u.addr;for (int i = 0; i < 10; i++){    long data = ptrace(PTRACE_PEEKDATA, c->pid, addr, 0);    for(int j = 0; j < 8; j++){        arr[8 * i + j] = data & 0x00000000000000ff;        data = ((data & 0xffffffffffffff00) >> 8);    }    addr += 8;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这样机器码会按地址的从小到大存放在<code>arr</code>中，便于反汇编成汇编代码，然后交给<code>zydis</code>解析即可</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">ZyanU64 runtime_address = c->u.addr;// Loop over the instructions in our buffer.ZyanUSize offset = 0;ZydisDisassembledInstruction instruction;while (ZYAN_SUCCESS(ZydisDisassembleIntel(    /* machine_mode:    */ ZYDIS_MACHINE_MODE_LONG_64,    /* runtime_address: */ runtime_address,    /* buffer:          */ arr + offset,    /* length:          */ sizeof(arr) - offset,    /* instruction:     */ &instruction))) {    printf("%016" PRIX64 "  %s\n", runtime_address, instruction.text);    offset += instruction.info.length;    runtime_address += instruction.info.length;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><img src="/posts/1234/image-20221024235051691.png" alt="Q3"></p><h2 id="Challenge"><a href="#Challenge" class="headerlink" title="Challenge:"></a>Challenge:</h2><p>Another feature missing from the <code>ssedb</code> is debugging symbols. For instance, when setting up breaking points, we’d like just to type a symbolic name, such as:</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">(ssedb) b main<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>instead of an ugly hexadecimal address for <code>main</code>. Implement this feature.</p><h3 id="Solution-3"><a href="#Solution-3" class="headerlink" title="Solution"></a>Solution</h3><p>调用BFD库，BFD库可以将符号和对应的值(地址)保存起来，这样如果输入一个符号，只需查询其地址即可</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">asymbol **symbol_table;    // symbol tablelong num_symbols;        // length// store symbol's name and valuevoid bfd_func(char *file){    long storage_needed;    bfd *abfd;    bfd_init(); // magic    abfd = bfd_openr(file, NULL);    assert(abfd != NULL);    bfd_check_format(abfd, bfd_object);    storage_needed = bfd_get_symtab_upper_bound(abfd);    assert(storage_needed >= 0);    symbol_table = (asymbol**)malloc(storage_needed);    assert(symbol_table != 0);    num_symbols = bfd_canonicalize_symtab(abfd, symbol_table);    assert(num_symbols >= 0);}// find symbol addresslong symbol_address(char *s){    symbol_info symbolinfo;    for(int i = 0; i < num_symbols; i++){        if (symbol_table[i]->section == NULL) continue;        bfd_symbol_info(symbol_table[i], &symbolinfo);        if (strcmp(s, symbolinfo.name))  continue;        printf("%s : 0x%x\n", symbolinfo.name, symbolinfo.value);        return (long)symbolinfo.value;    }}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在<code>main.c</code>中调用<code>bfd_func()</code>获得符号表，然后在<code>parseCommand</code>中判断输入是地址还是符号，如果是符号，则调用<code>symbol_address()</code>函数查找地址，顺便也实现了<code>x/i symbol</code> 和 <code>x/x symbol</code></p><p><img src="/posts/1234/image-20221025000155680.png" alt="Challenge"></p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本次实验整体不难，代码很全，难点是第三方库加入到工程中，需要一些<code>cmake</code>的知识。一开始将<code>exercese2</code>想的很简单，以为就前导0一个问题，不知道为什么要将第一个字节替换为<code>0xcc</code>，直到实现了<code>x/i</code>才知道，原来是需要一条<code>int 3</code>指令，使得子进程停止，那么问题就变得有意思了，再次返回父进程后我们需要将断点的原始信息还原。</p>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Sysprog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>github创建仓库并使用git上传本地代码</title>
      <link href="/posts/a5ca.html"/>
      <url>/posts/a5ca.html</url>
      
        <content type="html"><![CDATA[<h1 id="github创建仓库并使用git上传本地代码"><a href="#github创建仓库并使用git上传本地代码" class="headerlink" title="github创建仓库并使用git上传本地代码"></a>github创建仓库并使用git上传本地代码</h1><h2 id="创建本地仓库和github仓库"><a href="#创建本地仓库和github仓库" class="headerlink" title="创建本地仓库和github仓库"></a>创建本地仓库和github仓库</h2><p><img src="/posts/a5ca/QQ截图20220714151252.png" alt="创建本地仓库"></p><p>在Github上创建仓库并关联本地仓库，创建一个名为<code>hello</code>的github仓库</p><p><img src="/posts/a5ca/image-20220714151531421.png" alt="创建github仓库"></p><p>将标红处复制一下</p><p><img src="/posts/a5ca/image-20220714152055921.png" alt="仓库地址"></p><p>输入命令完成关联</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">git remote add origin git@github.com:shenhuagg555/hello.git<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>添加一个<code>README.md</code>文件来描述内容，使用<code>git add &lt;filename&gt;</code>添加文件</p><p><img src="/posts/a5ca/image-20220714152936684.png" alt="添加文件"></p><p>这里报了一个错误，需要我们设置邮箱和用户名，根据提示设置即可</p><p><img src="/posts/a5ca/image-20220714155736240.png" alt="配置邮箱和用户名"></p><h2 id="配置ssh"><a href="#配置ssh" class="headerlink" title="配置ssh"></a>配置ssh</h2><p>但是由于我们没有设置公钥，于是又出现了以下错误</p><p><img src="/posts/a5ca/image-20220714155832048.png" alt="禁止访问"></p><p>输入<code>ssh-keygen -t rsa</code>生成公钥和私钥，连续按三次回车生成，公钥和私钥保存在<code>~/.ssh</code> 里，再输入<code>cat ~/.ssh/id_rsa.pub</code>查看生成的公钥，将其复制，并在github上添加公钥即可。</p><p><img src="/posts/a5ca/image-20220714155147618.png" alt="配置ssh"></p><p>输入<code>git push -u origin master</code>完成本地代码上传，结果见下图</p><p><img src="/posts/a5ca/image-20220714155909456.png" alt="推送成功"></p><p><img src="/posts/a5ca/image-20220714154620817.png" alt="仓库页面"></p><p>从github服务器拉取最新的代码可以使用命令<code>git pull origin master</code></p><p><img src="/posts/a5ca/image-20220714164028692.png" alt="拉取代码"></p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">echo "# xx" >> README.mdgit initgit add README.mdgit commit -m "first commit"git remote add origin git@github.com:仓库地址.gitgit push -u origin master<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> 工具 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>WSL2下配置Anaconda、Pytorch、Jupyter Notebook</title>
      <link href="/posts/869.html"/>
      <url>/posts/869.html</url>
      
        <content type="html"><![CDATA[<h1 id="WSL2下配置Anaconda、Pytorch、Jupyter-Notebook"><a href="#WSL2下配置Anaconda、Pytorch、Jupyter-Notebook" class="headerlink" title="WSL2下配置Anaconda、Pytorch、Jupyter Notebook"></a>WSL2下配置Anaconda、Pytorch、Jupyter Notebook</h1><p>和原生Linux下配置环境基本一致</p><h2 id="1-Anaconda安装"><a href="#1-Anaconda安装" class="headerlink" title="1.Anaconda安装"></a>1.Anaconda安装</h2><p>去Anaconda的官网寻找最新的安装包，但这种方法下载速度太慢，可以寻找国内的源，如<a href="https://mirrors.tuna.tsinghua.edu.cn">清华源</a>，输入以下命令</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">wget https://mirrors.tuna.tsinghua.edu.cn/anaconda/archive/Anaconda3-2022.05-Linux-x86_64.sh<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>然后使用<code>bash</code>命令即可成功安装</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">bash Anaconda3-2022.05-Linux-x86_64.sh<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>创建一个虚拟环境来安装Pytorch</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda create -n torch python=3.9<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>启动环境</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda activate torch<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>关闭环境</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda deactivate torch<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="2-Pytorch安装"><a href="#2-Pytorch安装" class="headerlink" title="2.Pytorch安装"></a>2.Pytorch安装</h2><p>Pytorch官网给出了各种OS下的最新版本，电脑装了GPU的话就安装GPU版本的，选择如下，这里CUDA的版本需要自己查一下版本。</p><p><img src="/posts/869/image-20220628172251409.png" alt="Pytorch官网"></p><p>输入命令，查看CUDA版本：</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">nvidia-smi<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><img src="/posts/869/image-20220628172653467.png" alt="CUDA Version"></p><p>可以看到这里我的CUDA Version是11.6，将官网的命令修改一下版本即可，注意去掉-c pytorch，这样就可以使用国内源进行下载了，切换清华源如下：</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/conda config --set show_channel_urls yes<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>安装命令如下：</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda install pytorch torchvision torchaudio cudatoolkit=11.6<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这里有可能会安装为CPU版本的Pytorch，输入以下命令查看已经安装的库</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda list<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><img src="/posts/869/image-20220628173710455.png" alt="已安装的库"></p><p>如果是CPU版本的话，就要去清华源手动下载了，安装GPU版本的pytorch、torchvision</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda install https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/pytorch/linux-64/pytorch-1.12.0-py3.9_cuda11.6_cudnn8.3.2_0.tar.bz2conda install https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/pytorch/linux-64/torchvision-0.13.0-py39_cu116.tar.bz2<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>安装完毕，验证是否安装成功</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">import torchprint(torch.cuda.is_available()) #输出True即为安装成功<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="3-Jupyter-Notebook安装"><a href="#3-Jupyter-Notebook安装" class="headerlink" title="3.Jupyter Notebook安装"></a>3.Jupyter Notebook安装</h2><p>安装Jupyter Notebook比较简单，直接在新创建的torch环境中，输入以下命令</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda install jupyter notebook<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>添加内核</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">python -m ipykernel install --user<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>插件扩展</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda install -c conda-forge jupyter_contrib_nbextensions<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>安装主题</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">conda install jupyterthemes<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>一顿操作后，启动Jupyter Notebook时，发现无法显示网页链接，于是查看Jupyter Notebook配置，输入以下命令</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">jupyter notebook --generate-config<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>找到配置文件所在位置，找到<code>jupyter_notebook_config.py</code>，打开并搜索此句</p><pre class="line-numbers language-lang-text"><code class="language-lang-text">#c.NotebookApp.notebook_dir = ''<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>在该语句下面添加</p><pre class="line-numbers language-lang-python"><code class="language-lang-python">import webbrowserwebbrowser.register('edge',None,webbrowser.GenericBrowser(u'your webbrowser path'))c.NotebookApp.browser = 'edge'<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>之后启动Jupyter Notebook，输入</p><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">jupyter notebook<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>即可显示网页链接</p><p><img src="/posts/869/image-20220628175237834.png" alt="网页链接"></p><p>这样就可以愉快的使用Jupyter Notebook啦！！！</p><p><img src="/posts/869/image-20220628190717443.png" alt="jupyter notebook"></p><p>以上</p>]]></content>
      
      
      <categories>
          
          <category> 深度学习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Pytorch </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
